FROM httpd:2.4
COPY ./httpd.conf /usr/local/apache2/conf/httpd.conf
ENV DEBIAN_FRONTEND=noninteractive
RUN apt-get update
RUN apt-get install  -y --no-install-recommends \
wget \
unzip \
openssl \
gettext \
curl
CMD ["bash", "-c", "httpd -D FOREGROUND"]
